# MAAS 平台待办清单（2026-08-11 存档）

> 用途：中途休息存档，便于下次会话直接续做。
> 当前状态：**所有改动仅本地（dev server 运行中），未部署远程**。用户明确要求"除非允许，否则先别发布到远程"。

---

## ✅ 已完成（本轮，已本地验证）

| 事项 | 状态 |
|---|---|
| 系统管理全新模块（用户/角色/权限/监控/工单/日志/参数 7 子页 + 导航 + 路由） | ✅ 浏览器复检通过 |
| 系统管理 CRUD 补强：用户新增/编辑/删除、角色新增/删除、工单新建、监控重新拨测 | ✅ 复检通过（新增账号/角色/工单/拨测/删除均实测） |
| 退出登录白屏崩溃修复（hooks 早退位于后续 useEffect 之前） | ✅ 复检通过（退出→登录页→重新登录闭环正常） |
| 登录账号样例改为 6 位工号（100001/100238/…，含 MainLayout/UserPanel 引用同步） | ✅ 复检通过 |
| 全局光标修复：可点元素 hover 变手指、禁用态变禁止符（index.css 指针语义段） | ✅ 代码完成，**待全量复检** |

---

## ⏳ 待办（按优先级）

### P1. 登录页背景设计（用户已给参考）
- 参考：`http://172.1.0.86:5678/#/login`（猛禽/星舰智能登录页），截图已存 `doc/ref-login-full.png`
- 参考页设计要点（浏览器已提取）：
  - 左右分栏全幅布局，左侧紫蓝渐变插画区 + 标语"AI普惠-知识无界"（39px 白字 700 + 英文副标），右侧浅色区白卡表单
  - 登录按钮渐变 `#B29DF7 → #2563E9`，圆角 6px，48px 高；输入框 #F3F5F9 底、1px #EEEEEE 边、圆角 6px
  - 表单标题 "Make AI Happen"（27px #333 700）；底部双 Logo：信雅达® SUNYARD + 星舰智能 STARSHIP-AI
- 落点：`maas/src/layouts/MainLayout.tsx` 底部 `LoginScreen` 组件（当前为居中单卡，背景空白）
- 要求：科技感、体现平台核心功能（智能网关/弹性算力/国产化纳管）、不花里胡哨；我们的品牌词：星舰智能 / STARSHIP-MAAS

### P2. 菜单排序优化
- 原则：总览在前，日志类放最下面，按功能操作顺序渐进排序
- 需调整：`maas/src/layouts/MainLayout.tsx` 的 `NAV_TREE`
- 建议新顺序：运营驾驶舱 → 调度算力 → 模型资产 → 计量运营 → 策略治理 → 安全审计 → 系统管理 → 个人中心
  - 其中"调用日志/操作日志/审计日志"等日志类置于各组最末（调用日志已在计量运营末位附近，需核对）
  - 策略治理当前在第 2 位（偏早），建议后移（属于配置审批类）
  - 个人中心放最末（个人入口）

### P3. 网关 Agent 改名 + 样例能力丰富
- 落点：`maas/src/components/GatewayAgent.tsx`
- 改名建议：「算力网关 Agent」→「星舰 Copilot」/「平台智能助手」类命名（待用户确认用词）
- 样例丰富：现 QUICK 4 条 → 建议 6-8 条，增加：路由权重/配额全景/K8s Pod 状态/本月账单/今日拦截/灰度进度；意图分支同步扩（现 11 分支）
- 回复内容带真实数字（与各页数据口径一致，如 366 万/¥684,000/¥274 万节省）

### P4. 系统参数丰富
- 落点：`types/index.ts` SystemParams、`dataConfig.ts` systemParams、`api.ts` saveSystemParams 文案、`pages/system/OpsPanels.tsx` ParamsPanel
- 建议新增：登录 IP 白名单开关、数据脱敏开关、审计导出审批开关、密码历史不可重复次数、操作日志明细级别、通知渠道（站内/邮件/短信）、登录页公告开关等

### P5. 平台监控重构：分布式平台视角
- 用户反馈：平台监控没体现分布式，关注平台本身而非算力 GPU
- 落点：`pages/system/OpsPanels.tsx` MonitorPanel + `types/index.ts` PlatformService + 种子数据
- 建议：每个服务显示**多副本数、分布节点（如 gw-01/gw-02/gw-03）、版本、健康状态、时延**；顶部加服务拓扑/副本分布条；强调平台组件（网关集群/注册中心/计量/审计/队列/K8s 控制面），不展示 GPU 指标

### P6. 路由总览页审查优化
- 落点：`maas/src/pages/routing/RoutingPanel.tsx`（+ RoutingEnginePanel）
- 审查点：排版（KPI 行/Panel 布局）、功能完整性（路由决策表/漏斗/限流命中/白盒抽屉）、逻辑一致性（数字口径、跳转）、是否缺操作按钮

### P7. K8s 能力体现确认
- 现状：`maas/src/pages/routing/K8sPanel.tsx` 已挂载于算力总览（`/routing?view=compute`），含 3 集群卡（Volcano/GPU Operator/GPU 分配）+ 8 推理服务 Pod 表 + 滚动重启
- 待办：确认展示位置是否满足用户预期，必要时在平台监控 K8s 控制面卡与 K8sPanel 之间做联动说明

### P8. 收尾
- 全量 `npm run build` 零错误
- 浏览器逐页复检（含 cursor 指针、登录页、菜单顺序、Agent、监控、参数、路由页）
- 用户确认后 `py cicd/deploy.py --env sit` 部署上线 + 线上冒烟

---

## 📌 关键路径备忘

- 导航树/顶栏/登录页/账号菜单：`maas/src/layouts/MainLayout.tsx`
- 系统管理：`maas/src/pages/system/`（index / UserPanel / RolePermPanel / OpsPanels）
- 类型：`maas/src/types/index.ts`；种子数据：`maas/src/services/dataConfig.ts`；接口：`maas/src/services/api.ts`
- 全局样式（光标/主题）：`maas/src/index.css`
- 网关 Agent：`maas/src/components/GatewayAgent.tsx`
- K8s 面板：`maas/src/pages/routing/K8sPanel.tsx`；路由总览：`maas/src/pages/routing/RoutingPanel.tsx`
- 参考截图：`doc/ref-login-full.png`（用完可删）
- dev server：`http://localhost:5173/maas-web/`（后台运行中）；线上：`http://221.229.92.112:19095/maas-web/`（未部署最新）
